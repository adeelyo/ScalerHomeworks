This Package contains a tinyUrl maker. That takes any url in string format, converts it into a tinyUrl and keeps a map as complete data.

Could still be improved:
1. the map can be updated ina file as a database after every addition
2. only urls with https://www. are being considered.